# Checkpoint: Step 2 前端任务拆分 + 依赖编排与落盘组装

> **审查级别**：L2（完整审查）— 检查全部维度，含跨步骤一致性。
> **审查原则**：你是审查者，不是作者。必须重新打开产出文件逐项核对，不能凭记忆判断。
> **强制停止点**：这是交付点。汇总全部待确认项，停下来等待用户确认。

## 审查时需打开的文件
- 产出文件：`{TASK}/task-split.md`
- 上游比对：`{WORKSPACE}/task/01-backend-tasks.md`

## 章节完整性
- [ ] 包含本次任务拆分概览
- [ ] 包含后端任务清单（按模块/技术层次分区）
- [ ] 包含前端任务拆分原则与粒度约定
- [ ] 若 `frontend-web.md` 已生成：包含前端(PC Web)任务清单（按功能点分组）
- [ ] 若入口索引显示 `frontend-ios.md` 已生成：包含「## iOS 任务清单」；否则确认无残留 iOS 占位
- [ ] 若入口索引显示 `frontend-android.md` 已生成：包含「## Android 任务清单」；否则确认无残留 Android 占位
- [ ] 若入口索引显示 `frontend-hmos.md` 已生成：包含「## HMOS 任务清单」；否则确认无残留 HMOS 占位
- [ ] 若入口索引显示 `frontend-h5.md` 已生成：包含「## H5 任务清单」；否则确认无残留 H5 占位
- [ ] 包含依赖关系（任务级依赖表）
- [ ] 包含开放问题与需人工确认

## 前端任务清单检查
- [ ] 按功能点分组组织
- [ ] 每个功能点有独立章节
- [ ] 每个任务包含：任务标题、File、Work、Purpose
- [ ] 任务标题以动词开头
- [ ] 带依赖的任务标注了前置任务

## iOS / Android / HMOS / H5 任务清单检查（仅当对应分端设计文件已生成）
- [ ] 章节标题为 `## iOS 任务清单` / `## Android 任务清单` / `## HMOS 任务清单` / `## H5 任务清单`，功能点标题为 `## 功能点 N:`（可被 step-00 解析为 iOS-{N} / Android-{N} / HMOS-{N} / H5-{N}）
- [ ] 每个功能点与对应分端设计文件（`frontend-ios.md` / `frontend-android.md` / `frontend-hmos.md` / `frontend-h5.md`）的功能点一一对应
- [ ] 每个任务包含 File（落在对应端仓库路径）、Work、Purpose
- [ ] 依赖后端接口的 iOS/Android/HMOS/H5 任务已标注前置 `BE-{M}`

## 编号与覆盖
- [ ] 后端任务编号连续
- [ ] 前端任务按功能点分组，编号清晰
- [ ] 01-backend-tasks.md 中的每个工作包在任务清单中有对应任务
- [ ] 依赖关系表覆盖了所有有依赖的任务

## 一致性
- [ ] 后端任务与 architecture-design.md §5 应用内模块设计、backend-design.md 的模块划分一致
- [ ] 前端(PC Web)任务与 `frontend-web.md` 的页面/组件对应；各移动端任务与对应分端设计文件对应
- [ ] 依赖关系中引用的任务编号在清单中存在
- [ ] 开放问题指明了影响的任务编号

## 格式规范
- [ ] 表格列完整，无缺列
- [ ] Markdown 语法正确
- [ ] 全文 UTF-8

## 自检清单
- [ ] 清单可执行（每个任务有明确交付物和验证方式）
- [ ] 编号连续
- [ ] 依赖可理解
- [ ] 不确定项均进入"开放问题"

## 待确认项清零检查
- [ ] 终稿全文不包含 `<!-- STATUS_REPORT -->` 块
- [ ] 开放问题中每条指明了后端/前端(PC Web)/iOS/Android/HMOS/H5、影响任务、确认问题

## 待确认项提取
- 开放问题中的全部条目
- 全文中所有 `[需与产品确认]` 标记
- 覆盖检查中未覆盖的工作包
- 状态报告中的 corrections-made、manual-review-needed 和 unresolved-items
