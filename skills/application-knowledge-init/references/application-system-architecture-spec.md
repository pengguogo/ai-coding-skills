# 系统架构与调用拓扑规范

本文档定义 `application-system-architecture.md`（系统架构与调用拓扑）的完整输出模板、内容标准与生成约束。

---

## 1 文档定位

系统架构册侧重系统拓扑、分层图（Mermaid）、端到端调用关系与调用链说明，是三份应用架构文档中的"骨架"。

---

## 2 输出模板

```markdown
# {平台名称} — 系统架构与调用拓扑

## 1 系统全景

[一段文字概括系统核心业务、架构风格（如两代架构并行）、前端形态等]

## 2 顶层架构分层图

[使用 Mermaid graph TB 绘制，以应用组（AG）为 subgraph 边界，展示用户层→前端层→网关层→应用服务层→基础设施层→外部系统的宏观拓扑。对于超大项目，节点应抽象到子系统或核心应用级别]

## 3 端到端调用链

> 按链路类型分节，每个核心链路包含文字说明和 Mermaid 时序图。
> **时序图必须从前端入口开始绘制**（前端应用 → 网关/BFF → 后端服务 → 基础设施），完整展示一次用户请求从前端发起到后端处理的全链路流转，不得省略前端入口跳。

### 3.x [链路名称]

[文字说明请求链路的各跳转节点，从前端用户动作开始描述]

[使用 Mermaid sequenceDiagram 绘制该链路的时序图，第一个参与者为前端应用]

## 4 跨系统集成点

### 4.1 外部系统集成

| 外部系统 | 集成方式 | 提供方 | 消费方 | 说明 |
|---------|---------|--------|--------|------|
| [外部系统名] | [HTTP/EJB/MQ/TCP等] | [提供方] | [消费方APP-*] | [集成说明] |

### 4.2 内部系统间集成

| 集成关系 | 集成方式 | 提供方 | 消费方 | 说明 |
|---------|---------|--------|--------|------|
| [集成关系描述] | [HTTP/Feign/MQ/共享DB/共享缓存等] | [提供方APP-*] | [消费方APP-*] | [说明] |

### 4.3 前后端集成

> 按前端应用分组，列出其消费的后端能力与 API 路径族。路径族指共享同一前缀的一组 API（如 `/api/order/**`），用于快速定位前端请求对应的后端能力域。

| 前端应用 | API 路径族 | 后端应用 | 后端能力语义 | 集成方式 | 说明 |
|---------|-----------|---------|------------|---------|------|
| [APP-* 前端] | [/api/xxx/**] | [APP-* 后端] | [该路径族对应的业务能力，如"订单查询与管理"] | [HTTP] | [补充说明，如网关转发规则] |

## 5 部署架构概要

### 5.1 后端部署

[按架构风格分组，列出每个可部署单元的部署形态]

| 应用/WAR包 | 职责 | 部署形态 | 默认端口 | 网络区域 |
|-----------|------|---------|---------|---------|
| [APP-* 或 WAR 名] | [职责] | [JAR/WAR/容器] | [端口] | [内网/DMZ] |

### 5.2 新架构部署 (按需)

| 应用 | 默认端口 | 部署形态 |
|------|---------|---------|
| [APP-*] | [端口] | [Spring Boot JAR/WAR] |

### 5.3 前端部署

[说明前端应用的构建产物和分发方式（Nginx/CDN/随WAR包等）]

## 6 多体系并行 / 双链架构对比 (可选)

> 当平台存在多套并行的技术体系/实现路径（如新一代核心与标准老核心并存、新旧架构双链运行）时，用本节做结构化对比。仅描述**系统实现层面**的差异（承载子系统、流程编排方式、接入承载、数据归属、演进策略等），不重复业务全景册的 BG/B/S 业务语义。

| 维度 | 链路A：[体系A名称（含承载 APP-*）] | 链路B：[体系B名称（含承载 APP-*）] |
|------|-----------------------------------|-----------------------------------|
| [对比维度，如流程编排/接入承载/数据归属] | [链路A实现描述] | [链路B实现描述] |

> 并存与演进策略说明：[说明两套体系的运行期关系、新功能默认落地哪一套、数据是否共享、迁移/收敛计划等]

## 真相源与参考路径

> 列出本文档聚合所依据的全部子系统代码基线路径，确保可追溯。严禁编造不存在的 markdown 文件。

本文档基于以下子系统代码基线聚合生成：

- `ocspec-<xxx>/knowledge/code/<子系统A>/`（backend-project.md / backend-interface / backend-external-dependency.md / backend-database）
- `ocspec-<xxx>/knowledge/code/<子系统B>/`（...）
- `ocspec-<xxx>/knowledge/code/<前端项目A>/frontend-project.md`
- `ocspec-<xxx>/knowledge/code/<前端项目B>/frontend-project.md`
- `ocspec-<xxx>/knowledge/business/`（S-*/DO-* 编号体系引用，若存在）
- `ocspec-<xxx>/knowledge/_workspace/app/01-scope.md`（范围分析产物）
- `ocspec-<xxx>/knowledge/_workspace/app/02-topology.md`（拓扑分析产物，AG/APP/AC/S/DO 编号定义源）
```

---

## 3 内容标准与填表指南

- **宏观与微观分离**：对于包含 8+ 子系统的项目，架构图必须体现宏观（子系统级别）交互，将详细的内部交互下放到组件册或单独的局部视图中。
- **调用关系与串联**：重点梳理**跨子系统**的集成点。**注意区分"调用(RPC/HTTP)"和"依赖(如共用数据库/缓存)"**。如果是 MQ 异步通知，必须写明 Topic 或 Event 语义。
- **前后端集成（§4.3）**：路径族从前端代码基线（`frontend-project.md`）中提取 API 前缀归纳而来，后端能力语义从后端代码基线或应用架构上下文推导。路径族粒度以共享同一业务含义的前缀为准（如 `/api/order/**`），不要逐个接口罗列。无法确认后端能力语义时标记 `[需人工确认]`。
- **多体系并行对比（§6，可选）**：仅当系统存在多套并行技术体系/实现路径时输出。对比维度聚焦系统实现层（承载 APP-*、流程编排方式、接入承载、数据归属、演进策略），并标注每条链路的承载子系统编号（APP-*/AG-*）。业务语义（BG/B/S）不在本节展开，如需引用业务场景仅引用 S-* 编号，不重复业务全景册内容。

---

## 4 生成约束

- Mermaid 图表必须符合标准语义（分层、调用、时序），避免生成包含超过 30 个节点的单张超大图表导致渲染失败。
- **端到端调用链时序图必须覆盖所有核心业务链路**，不得遗漏。判断标准：每个独立的请求入口类型和每个核心业务场景都应有对应的时序图。当系统存在新旧两套架构时，关键链路需分别绘制新架构和传统架构的时序图。**时序图必须从前端入口开始**，完整展示用户请求从前端发起到后端处理的全链路，不得省略前端入口跳。
- 当系统存在多套架构并行时，§6 的多体系并行对比表须与 §1 系统全景的架构风格描述、及 §3 端到端调用链中新旧架构分别绘制的时序图保持一致；链路命名（链路A/链路B）在全文中须统一。
- **顶层架构图中的 subgraph 必须标注 AG 编号**（如 `subgraph AG-01 [应用组名称]`），确保图中的分组与主数据册的 AG 台账一致。
- 所有表格列必须与规范完全一致，不得随意删减列。如果某列无数据，填「-」或「不适用」。
- `真相源与参考路径` 必须真实引用所有参与聚合的子系统基线。严禁编造不存在的 markdown 文件。
