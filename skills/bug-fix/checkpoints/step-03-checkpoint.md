# Checkpoint: Step 3 修复方案与影响面冻结

> **审查级别**：L1（结构 + 内容）。重新打开产出文件逐项核对，不凭记忆。

## 审查时需打开的文件
- 产出：`{BASE_DIR}/bug/<id>-<slug>-fix-plan.md`（持久留底）
- 上游：`{WORKSPACE}/bugfix/02-locate-and-impact.md`
- 规范：`references/bug-triage-standard.md`、`references/fix-plan-template.md`

## 结构检查
- [ ] 含 `## 修复方案` 章节锚点（供 L3 code-reviewer 引用）
- [ ] 含模板全部章节：修复方案表（含基线出处列）/ 允许改动文件清单（三列）/ 关联风险与影响 / 受影响模块与编译命令 / 验证计划 / 越界检查表

## 内容检查
- [ ] 每条修复决策对应 Step 2 根因（文件+行号可追溯）
- [ ] 每条修复决策「基线出处」列非空，且为 设计章节 / REQ 编号 / 知识库文件 / `[无设计基线-代码现状推断]` 之一
- [ ] `[无设计基线-代码现状推断]` 仅在 Step 1 判为无设计基线时出现；有设计基线却用该值 → FAIL
- [ ] 允许改动清单非空；路径相对仓库根、仓库归属用独立列
- [ ] 关联风险与影响已填（间接影响/数据/兼容性/回归风险点/同源缺陷，无则显式写「无」）
- [ ] 纯逻辑 bug 且有测试框架时，新增单测文件已纳入允许清单
- [ ] 受影响模块 + 建议编译命令已给出
- [ ] 验证计划齐全：静态自证必有；纯逻辑+有框架→单测计划+命令；纯逻辑+无框架→`[无测试基建]`；运行时类→人工回归点清单
- [ ] 越界检查已执行；命中接口/DDL/新模块/新依赖/超阈值 → STATUS_REPORT 为 BLOCKED
- [ ] 无设计基线时方案顶部有 `⚠️` 风险横幅
- [ ] 推理正文无绝对路径、无写死仓库名（允许清单的仓库名/相对路径、编译命令、基线出处豁免）

## 待确认项提取
- 越界 BLOCKED 升级说明 / 状态报告 concerns / blocker
