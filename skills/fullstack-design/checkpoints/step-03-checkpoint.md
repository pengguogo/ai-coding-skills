# Checkpoint: Step 3 §1 背景与目标 + §2 技术选型 + §3.1 模型设计

> **审查级别**：L1（轻量审查）— 只检查结构和内容维度，跨步骤一致性延迟到 Step 5 的 L2 审查。
> **审查原则**：你是审查者，不是作者。必须重新打开产出文件逐项核对，不能凭记忆判断。

## 审查时需打开的文件
- 产出文件：`{WORKSPACE}/design/03-backend-foundation.md`
- 上游比对：`{WORKSPACE}/design/01-input-summary.md`、`{DESIGN}/architecture-design.md`

## §1 结构检查
- [ ] 包含 §1.1 背景（含业务场景、当前问题、业务价值）
- [ ] 包含 §1.2 目标（含至少一个可度量指标）
- [ ] 包含 §1.3 约束
- [ ] 包含 §1.4 参考文档（引用了需求文档实际路径 + architecture-design.md）
- [ ] 包含 §1.5 路径与配置类约定（与场景判定一致）

## §2 结构检查
- [ ] 包含 §2 技术选型（覆盖数据库、缓存、消息队列）
- [ ] §2 含导航行"应用架构见 architecture-design.md §2、模块清单见 §5，本文不重列"（架构/模块不在后端文档重复）

## §3.1 结构检查
- [ ] 包含 §3.1.1 领域模型（至少 1 个实体，含属性和行为）
- [ ] 包含 §3.1.2 数据模型（每张表有字段定义表格、建表 SQL、Mermaid erDiagram）
- [ ] 包含 §3.1.3 状态机（或"本期无状态机设计"）

## 改动标注检查（L1，跑存在性 + 反极端，按 change-annotation-standard）
- [ ] §3.1.2 每个 erDiagram 下方有「数据模型改动清单表」（仅收 DDL）；表标题含 `[新增]/[改造]/[复用]`；`[新增]` 全表免逐字段标，`[改造]` 表变更字段已标
- [ ] §3.1.3 若有状态机：改动的状态/转换有 `[新增]/[改造]` 文字标签；整机全新仅图下声明、不逐条标（标"本期无状态机设计"时本项跳过）
- [ ] 整体全改走图下「改动标注约定」声明块（图内零逐元素标签 + 有声明块 → 合格，不算 0% 漏标）；逐元素全标 → 提示精简（`DONE_WITH_CONCERNS`，非 FAIL）；既无标签又无声明块 → 真漏标 FAIL

## 内容一致性检查
- [ ] §3.1/§3.2 引用的模块名与 architecture-design.md §5 应用内模块设计一致
- [ ] 技术栈与 01-input-summary.md 的工程约束一致（存量项目）
- [ ] 数据库类型与 §2 技术选型一致
- [ ] §3.1.2 数据模型的表与 architecture-design.md §6 ER 总览的表归属无矛盾

## 待确认项提取
- 产出正文中所有 `[需与产品确认]` 标记
- 状态报告中的 concerns / missing-context
