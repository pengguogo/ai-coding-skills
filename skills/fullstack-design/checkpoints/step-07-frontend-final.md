# Checkpoint: Step 7 前端设计最终校验

> **审查级别**：L2（完整审查）— 检查全部维度，含接口对齐一致性和跨步骤一致性。
> **审查原则**：你是审查者，不是作者。必须重新打开产出文件逐项核对，不能凭记忆判断。
> **强制停止点**：这是前端阶段的交付点。汇总全部待确认项，停下来等待用户确认。

## 审查时需打开的文件

- 入口文件：`{DESIGN}/frontend-design.md`
- 分端设计文件（本次已生成的端）：`{DESIGN}/frontend-web.md` / `frontend-ios.md` / `frontend-android.md` / `frontend-hmos.md` / `frontend-h5.md`
- 上游比对：`{DESIGN}/backend-design.md`、`{WORKSPACE}/design/06-frontend-analysis.md`

## 入口文件（frontend-design.md）检查

- [ ] 入口文件已生成（无论涉及几个目标端，均须存在）
- [ ] 为 PC Web、iOS、Android、HMOS、H5 五端各设置访问章节
- [ ] 每个访问章节说明：分端设计文件名、访问方式、引用工程、使用条件（何时用/何时不用）
- [ ] 已生成分端设计文件的端标注为「已生成」；未生成的端标注为「本次未生成」
- [ ] 入口文件不含分端设计正文（仅索引）

## 分端设计文件章节完整性（对每个已生成的分端设计文件抽查）

- [ ] Web 端 `frontend-web.md`：含"一、概述""二、架构说明""三、规范文档对齐""四、功能逻辑实现"（至少 1 个功能点）
- [ ] iOS/Android/HMOS 端：含对应端标准的"一～四"章节，功能点标题为 `### 功能点 N：{名称}`
- [ ] H5 端 `frontend-h5.md`：含"一～四"章节，覆盖移动端适配
- [ ] 每个功能点包含：功能概述、数据与接口、实现方案（组件/界面结构+交互流程+状态定义）
- [ ] 分端设计文件命名与约定一致（`frontend-web.md` / `frontend-ios.md` / `frontend-android.md` / `frontend-hmos.md` / `frontend-h5.md`）

## 字段与组件粒度（逐功能点核对）

- [ ] 数据类型有完整 TypeScript interface（逐字段，含类型和注释）
- [ ] 接口入参/出参有字段表，出参展开到叶子字段
- [ ] 子组件有 Props/Events 定义表
- [ ] 有用户操作清单（逐一列出可交互元素及处理流程）
- [ ] 表单有字段校验规则表；列表有表格列定义表（如适用）
- [ ] 每个涉及接口的功能点，交互流程图含异常路径（错误态、空态、超时）
- [ ] 表单类功能点有完整字段校验规则表（含字段名、校验规则、错误提示文案）

## 接口对齐自检（全部通过才算合格）

- [ ] 1. 接口覆盖完整性
- [ ] 2. 入参字段逐一对齐
- [ ] 3. 出参字段逐一对齐
- [ ] 4. 来源标注
- [ ] 5. TS interface 与后端一致
- [ ] 6. 新增/改造字段标注（出参标记列用 `[新增]/[改造]/—`，不残留 `[变更]`）
- [ ] 7. 无臆造接口

## 改动标注检查（L2，全量：存在性 + 差集核对 + 反极端，按 change-annotation-standard）

- [ ] §4.3 交互流程图中本期新增/改造的交互有标注（时序图用 `rect` 着色 / 流程图用 `:::chg-*` 着色 / 或文字前缀）；复用不标
- [ ] §4.4 状态流转图中改动的状态/转换有 `[新增]/[改造]` 文字前缀（如涉及）
- [ ] 出参字段表「标记」列三态用词与后端、架构一致（统一为 `[新增]/[改造]`）
- [ ] 整体全改走图下声明块（图内零标签 + 有声明块 → 合格，不算 0% 漏标）；逐元素全标 → 提示精简（`DONE_WITH_CONCERNS`，非 FAIL）；既无标签又无声明块 → 真漏标 FAIL

## 格式规范

- [ ] Mermaid 图表语法正确
- [ ] 所有表格列完整
- [ ] 全文 UTF-8

## 待确认项清零检查（终稿强制，覆盖入口文件与全部已生成分端设计文件）

- [ ] 全文不包含 `[需与产品确认]` 标记
- [ ] 全文不包含 `[需人工确认]` 标记
- [ ] 全文不包含 `[需后端设计补充]` 标记
- [ ] 全文不包含 `<!-- STATUS_REPORT -->` 块

## 待确认项提取

- 待确认项清零检查中任何未通过的项
- 接口对齐自检中任何未通过的项（不可自动修正的）
- 全文中所有 `[需人工确认]`、`[需后端设计补充]` 标记（含位置）
- 状态报告中的 corrections-made、manual-review-needed 和 unresolved-items
