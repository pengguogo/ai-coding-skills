# Checkpoint: Step 5 后端设计最终校验

> **审查级别**：L2（完整审查）— 检查全部维度，含跨步骤一致性和跨功能引用一致性。
> **审查原则**：你是审查者，不是作者。必须重新打开产出文件逐项核对，不能凭记忆判断。
> **强制停止点**：这是后端阶段的交付点。汇总全部待确认项，停下来等待用户确认。

## 审查时需打开的文件
- 产出文件：`{DESIGN}/backend-design.md`
- 上游比对：`{WORKSPACE}/design/03-backend-foundation.md`、`{WORKSPACE}/design/01-input-summary.md`、`{DESIGN}/architecture-design.md`、`{REQ}/requirement.md`

## 章节完整性
- [ ] 包含 §1 背景与目标（§1.1 ~ §1.5）
- [ ] 包含 §2 技术选型（含导航至 architecture-design.md §2 架构 / §5 模块清单）
- [ ] 包含 §3.1 模型设计（§3.1.1 领域模型、§3.1.2 数据模型、§3.1.3 状态机或"无"）
- [ ] 包含 §3.2 功能设计（至少 1 个功能模块，每个功能含边界值与异常值处理子节或"无"）
- [ ] 包含 §4 稳定性&安全评估（§4.1 + §4.2）
- [ ] 包含 §5 风险评估（§5.1 + §5.2）
- [ ] 包含 §6 开放问题（或"无开放问题"）

## 术语一致性
- [ ] §3.1/§3.2 中出现的模块名，与 architecture-design.md §5 应用内模块设计一致（模块名权威源在架构文档，后端文档无独立模块章）
- [ ] 实体名称在 §3.1.1 和 §3.1.2 中一一对应
- [ ] 表名在 §3.1.2 和 §3.2 的"数据库"小节中一致

## 架构文档一致性
- [ ] §3.1.2 数据模型的表，与 architecture-design.md §6 ER 总览的表归属一致（无矛盾）
- [ ] §3.2 功能涉及的应用，与 architecture-design.md §3 改造矩阵一致
- [ ] 后端接口/表三态，与架构 §3 改造矩阵、§6 ER 总览对同一对象的三态判定不冲突（如矩阵标复用、后端不得标新增）

## 改动标注完整性（L2，全量：存在性 + 差集核对 + 反极端，按 change-annotation-standard）
- [ ] 时序图：改动交互用 `rect`（新增绿/改造黄）包裹，每个 rect 块说明非空、描述了改动内容；整图全改有声明
- [ ] 接口：每个对外/依赖接口标题含三态；每个 `[改造]` 接口有改造点说明且命中路径/入参/出参/核心逻辑之一（空壳不通过）；每个 `[复用]` 接口标了现网出处（否则判"疑似臆造复用"）
- [ ] 数据模型：每个 erDiagram 下有「数据模型改动清单表」（仅收 DDL）；`[改造]` 表的变更字段与 erDiagram 内字段注释标签一一对应（`[新增]` 全表免逐字段标，改动字段列写"全表"即通过）；表标题含三态
- [ ] 状态机（若有）：改动状态/转换有 `[新增]/[改造]` 标签，声明块列出的改动转换数与图内标签数一致；整机全新仅图下声明、不逐条标（标"本期无状态机设计"时跳过）
- [ ] 全文不存在 `<span style="color:red">` 行内红字标注
- [ ] 整体全改走图下声明块（图内零标签 + 有声明块 → 合格，不算 0% 漏标）；逐元素全标 → 提示精简（`DONE_WITH_CONCERNS`，非 FAIL）；既无标签又无声明块 → 真漏标 FAIL；存量需求若接口全 `[新增]`、零 `[改造]`/`[复用]`，复核是否漏判

## 接口完整性（为前端阶段服务）
- [ ] 每个对外接口有入参字段表格
- [ ] 每个对外接口有出参字段表格
- [ ] 不存在仅有 JSON 示例而无字段表格的接口

## 需求覆盖
- [ ] 功能清单中的每个 P0 需求项在 §3.2 中有对应功能设计
- [ ] 功能清单中的每个 P1 需求项在 §3.2 中有对应功能设计或在 §6 中说明延后原因

## 跨功能引用一致性
- [ ] 功能 A 的时序图中调用功能 B 的接口时，其 URL/HTTP 方法/参数与功能 B 的对外接口定义一致
- [ ] 不同功能中引用同一实体时，字段名和类型与 §3.1 数据模型一致
- [ ] 功能清单中标注了跨功能依赖的条目，在对应功能的"依赖接口"中有体现

## 接口来源可追溯性检查（严禁臆造接口）
- [ ] §3.2 中每个"依赖接口"均可追溯到以下来源之一：本文档内已定义的对外接口、01-input-summary.md 外部依赖表、需求文档明确提及的已有接口
- [ ] 不存在无来源依据的臆造接口
- [ ] 所有无法确认存在的接口均已标注 `[需人工确认]` 并说明假设依据
- [ ] §3.2 中"对外接口"的 URL 和字段定义未引用项目中不存在的路径或模块

## 稳定性与风险检查
- [ ] §4.1.1 限流：每个对外接口均有限流判定（限流写明维度/阈值/算法，不限流写明理由）
- [ ] §4.1.2 降级熔断：每个外部依赖均有降级与熔断策略
- [ ] §5 每条风险含触发条件、影响范围、可落地应对措施、责任边界四要素
- [ ] 开放问题每条有：问题描述、影响范围、待决策内容

## 边界值与异常值处理检查
- [ ] 01-input-summary.md §3 中每条边界与异常线索，在 §3.2 对应功能中有处理落点或明确的不适用理由（不得静默跳过）
- [ ] 边界设计达到 design-backend-standard §3.2 深度要求（含错误码、阈值、策略，具体可执行）
- [ ] §3.2 每个功能的"异常处理与重试补偿"中包含边界值与异常值处理，以及重试补偿机制
- [ ] 涉及数值型入参的接口，已说明最大/最小值的校验逻辑及超出时的错误码
- [ ] 涉及必填字段的接口，已说明空值校验与错误响应
- [ ] 涉及多字段关联的接口（如时间范围、数值区间、互斥参数），已说明组合入参交叉约束的校验逻辑及错误码
- [ ] 涉及批量操作的接口，已说明最大条数限制、超限拒绝/分批策略、空列表处理
- [ ] 涉及业务规则的功能（如金额、数量、时间范围），已说明业务边界值的处理
- [ ] 涉及外部系统调用的功能，已说明外部异常值（空响应、超长字段、非法状态码）的防御处理

## 格式规范
- [ ] Mermaid 图表语法正确
- [ ] 所有表格列完整，无缺列
- [ ] 代码块有语言标注
- [ ] 全文 UTF-8

## 待确认项清零检查（终稿强制）
- [ ] 终稿全文不包含 `[需与产品确认]` 标记
- [ ] 终稿全文不包含 `[需人工确认]` 标记
- [ ] 终稿全文不包含 `<!-- STATUS_REPORT -->` 块

## 待确认项提取
- 待确认项清零检查中任何未通过的项
- 全文中所有 `[需人工确认]`、`[需与产品确认]` 标记（含位置）
- §6 开放问题中的每一条
- 需求覆盖检查中未覆盖的 P1 需求项
- 术语/接口不一致中无法自动修正的项
- 状态报告中的 corrections-made、manual-review-needed 和 unresolved-items
