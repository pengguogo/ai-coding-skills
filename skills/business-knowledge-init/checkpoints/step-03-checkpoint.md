# Checkpoint: Agent-03 逐场景流程建模与用例推导

> **审查级别**：L1（轻量审查）— 只检查结构和内容维度，一致性和完整性延迟到步骤 6 的 L2 审查。
> **审查原则**：你是审查者，不是作者。必须重新打开产出文件逐项核对，不能凭记忆判断。

## 审查时需打开的文件
- 产出文件：`{WORKSPACE}/biz/03-process.md`（或合并后的完整产出）

## 结构检查
- [ ] 每个 S-* 场景包含子章节：参与者清单
- [ ] 每个 S-* 场景包含子章节：流程描述表
- [ ] 每个 S-* 场景包含子章节：可变点（VP）
- [ ] 每个 S-* 场景包含子章节：流程图（Mermaid flowchart TD）
- [ ] 每个 S-* 场景包含子章节：用例清单

## 内容检查
- [ ] 每个 S-* 至少有一条主流程
- [ ] 流程层级四级完整：阶段 → 活动 → 任务 → 步骤（不得省略任务和步骤层级）
- [ ] 流程中不包含技术实现步骤（如"调用 XXX 接口"不是业务步骤）
- [ ] 每个场景有参与者清单，包含参与者名、类型、在本场景中的角色
- [ ] 每个场景有用例清单，包含 UC 编号、名称、参与者、说明
- [ ] 状态变更列已标注（供 Step 4 领域建模使用）
- [ ] 合并后参与者无重复（同名参与者合并为一条）
- [ ] 合并后 UC 编号全局唯一（无重复编号）
- [ ] 跨场景触发关系表非空（若存在场景间依赖）

## 待确认项提取
审查完成后，从产出中提取以下内容作为待确认项：
- 产出正文中所有 `[需人工确认]` 标记
- 状态报告中的 concerns / missing-context / items-skipped
