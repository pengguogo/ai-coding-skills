# Checkpoint: Agent-04 领域建模与流程-事件串联

> **审查级别**：L1（轻量审查）— 只检查结构和内容维度，一致性和完整性延迟到步骤 6 的 L2 审查。
> **审查原则**：你是审查者，不是作者。必须重新打开产出文件逐项核对，不能凭记忆判断。

## 审查时需打开的文件
- 产出文件：`{WORKSPACE}/biz/04-domain.md`

## 结构检查
- [ ] 包含章节：子域（SD）清单（§1）
- [ ] 包含章节：领域对象（DO）清单（§2）
- [ ] 包含章节：领域事件（DE）清单（§3）
- [ ] 包含章节：领域模型关系图（§4），含 Mermaid erDiagram
- [ ] 包含章节：流程-事件串联表（§5）
- [ ] 包含章节：与 application 侧 DO 互证（§6）
- [ ] 包含章节：待确认清单（§7）

## 内容检查
- [ ] SD 清单至少有 1 行数据，每行包含 SD 编号、名称、职责、包含 DO、所属 BG
- [ ] DO 清单至少有 1 行数据，每行包含 DO 编号、名称、管理子域、类型、来源、与 application 侧对齐
- [ ] DO 粒度合理：异常处理记录、审批记录、结案记录、财务交易记录等状态变更产物有独立 DO
- [ ] DO 中不包含数据库物理表名或字段名
- [ ] DE 清单至少有 1 行数据，每行包含 DE 编号、名称、触发场景、触发活动、关联 DO、事件类型、来源
- [ ] DE 粒度合理：异常处理完成、审批通过/拒绝、零值结案等分支路径有独立 DE
- [ ] 领域模型关系图中的 DO 编号与 DO 清单一致
- [ ] 流程-事件串联表中每个 DE 有明确的触发流程活动（闭环）
- [ ] 若 application 基线存在，DO 互证表非空

## 待确认项提取
审查完成后，从产出中提取以下内容作为待确认项：
- 待确认清单（§7）中的每一条
- 产出正文中所有 `[需人工确认]` 标记
- 隐式事件标注（"隐式事件（代码中无显式 MQ）"）
- 状态报告中的 concerns / missing-context
